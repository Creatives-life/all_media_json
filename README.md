# all_media_json  
A GUI‑application to convert multiple media types (image, audio, video, etc.) into Base64 strings and export them to JSON format. Built with Python and a web interface for ease of use.

## Features  
- Supports **images**, **audio**, **video**, and **any other file types**.  
- Converts the media files into Base64‑encoded strings.  
- Generates a JSON output containing file metadata and the Base64 data.  
- Web interface for selecting files, setting options, and downloading the JSON output.  
- Designed for simplicity and automation of media handling for projects like NFTs, metadata generation, and more.

## Tech Stack  
- Python (backend logic)  
- Flask (web server)  
- HTML/CSS/JavaScript (front‑end interface)  
- Additional Python libraries (see `requirements.txt`)  

## 🛠️ Setup & Local Use  
1. Clone the repository:
   ```bash
   git clone https://github.com/Creatives‑life/all_media_json.git
   cd all_media_json
